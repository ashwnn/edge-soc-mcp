{
  "name": "edge-soc-mcp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "bun test",
    "cf-typegen": "wrangler types",
    "seed": "bun run scripts/seed-corpora.ts"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.17.0",
    "fflate": "^0.8.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.20",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.105.0"
  }
}
